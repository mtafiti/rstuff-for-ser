
  cat("\nSerena - Loading R basics for your charting (rCharts, etc)..... \n",sep="")
  cat("———————————\n\n",sep="")
  
  options(rcharts.cdn = TRUE)
  #require(rCharts) #for charts
  library(rCharts)
  
  library(jsonlite) #for NDJSON. see https://jeroenooms.github.io/mongo-slides/#18
  #saliva_scoped1 <- stream_in(file('scoped/saliva/serena_log.SERVER.SCOPED.15.03.log'
  
  library(stargazer) #stargazer(mydata, type = "text", title="Descriptive statistics", digits=1, out="table1.txt")
  
  library(rjson)
  
  library(plyr) #for filter, joins
  
  library(jsonlite)  #for: flatten(dall)
  
  source("serenaCharting.R")
  source("myutils.R")
  
  print("R profile for Serena charts loaded!")
  cat("———————————\n\n",sep="")
  
  